# ── API ───────────────────────────────────────────────────────────────────────
# Base URL for the HisabKitab backend API (no trailing slash)
# Expo requires the EXPO_PUBLIC_ prefix for variables accessible in client code
EXPO_PUBLIC_API_URL=https://your-api-url.com/api
